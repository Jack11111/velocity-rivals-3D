import * as THREE from 'three';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
const $=id=>document.getElementById(id);const game=$('game'),rankEl=$('rank'),progressEl=$('progress'),speedEl=document.querySelector('#speed b'),itemBtn=$('item'),msgEl=$('message'),menu=$('menu'),playBtn=$('play'),loadingEl=$('loading');
const scene=new THREE.Scene();scene.background=new THREE.Color(0x72c7ff);scene.fog=new THREE.FogExp2(0xb9ddf4,.0062);const camera=new THREE.PerspectiveCamera(74,innerWidth/innerHeight,.1,850);const renderer=new THREE.WebGLRenderer({antialias:true,powerPreference:'high-performance'});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);renderer.shadowMap.enabled=true;renderer.shadowMap.type=THREE.PCFSoftShadowMap;renderer.outputColorSpace=THREE.SRGBColorSpace;renderer.toneMapping=THREE.ACESFilmicToneMapping;renderer.toneMappingExposure=1.24;game.appendChild(renderer.domElement);
scene.add(new THREE.HemisphereLight(0xe9f7ff,0x4d704a,3.2));const sun=new THREE.DirectionalLight(0xfff1d1,5.2);sun.position.set(-55,85,35);sun.castShadow=true;sun.shadow.mapSize.set(2048,2048);sun.shadow.camera.left=-70;sun.shadow.camera.right=70;sun.shadow.camera.top=70;sun.shadow.camera.bottom=-70;sun.shadow.bias=-.00012;scene.add(sun);const fill=new THREE.DirectionalLight(0x69b7ff,1.25);fill.position.set(40,24,-30);scene.add(fill);
const world=new THREE.Group();scene.add(world);const points=[new THREE.Vector3(0,0,0),new THREE.Vector3(0,0,-65),new THREE.Vector3(-20,2,-125),new THREE.Vector3(-48,4,-180),new THREE.Vector3(-25,7,-245),new THREE.Vector3(22,5,-300),new THREE.Vector3(54,2,-365),new THREE.Vector3(38,0,-430),new THREE.Vector3(-10,1,-495),new THREE.Vector3(-45,3,-555),new THREE.Vector3(-18,5,-625),new THREE.Vector3(27,2,-690),new THREE.Vector3(0,0,-770)];const curve=new THREE.CatmullRomCurve3(points,false,'catmullrom',.35);function basisAt(t){t=THREE.MathUtils.clamp(t,0,1);const p=curve.getPointAt(t),tangent=curve.getTangentAt(t).normalize(),up=new THREE.Vector3(0,1,0),right=new THREE.Vector3().crossVectors(tangent,up).normalize(),normal=new THREE.Vector3().crossVectors(right,tangent).normalize();return{p,tangent,right,normal}}function orient(mesh,b){mesh.lookAt(mesh.position.clone().add(b.tangent))}function buildRibbon(width,mat,y=.02){const segs=360,pos=[],idx=[];for(let i=0;i<=segs;i++){const b=basisAt(i/segs),c=b.p.clone().addScaledVector(b.normal,y),l=c.clone().addScaledVector(b.right,-width/2),r=c.clone().addScaledVector(b.right,width/2);pos.push(l.x,l.y,l.z,r.x,r.y,r.z)}for(let i=0;i<segs;i++){const a=i*2;idx.push(a,a+2,a+1,a+1,a+2,a+3)}const g=new THREE.BufferGeometry();g.setAttribute('position',new THREE.Float32BufferAttribute(pos,3));g.setIndex(idx);g.computeVertexNormals();const m=new THREE.Mesh(g,mat);m.receiveShadow=true;world.add(m)}
const shoulderMat=new THREE.MeshStandardMaterial({color:0xe9eef5,roughness:.6,metalness:.03}),roadMat=new THREE.MeshPhysicalMaterial({color:0x313a47,roughness:.48,metalness:.08,clearcoat:.16,clearcoatRoughness:.68});buildRibbon(18.4,shoulderMat,.006);buildRibbon(15.8,roadMat,.028);const water=new THREE.Mesh(new THREE.PlaneGeometry(900,1150),new THREE.MeshPhysicalMaterial({color:0x159eea,roughness:.24,metalness:.06,transparent:true,opacity:.92}));water.rotation.x=-Math.PI/2;water.position.set(180,-3.2,-390);world.add(water);const land=new THREE.Mesh(new THREE.PlaneGeometry(900,1150),new THREE.MeshStandardMaterial({color:0x50a55d,roughness:1}));land.rotation.x=-Math.PI/2;land.position.set(-270,-.35,-390);land.receiveShadow=true;world.add(land);
const curbRed=new THREE.MeshStandardMaterial({color:0xf34655,roughness:.38}),curbWhite=new THREE.MeshStandardMaterial({color:0xffffff,roughness:.38}),curbGeo=new THREE.BoxGeometry(2.1,.14,1.3);for(let i=0;i<170;i++){const b=basisAt(i/169);for(const side of[-1,1]){const m=new THREE.Mesh(curbGeo,i%2?curbRed:curbWhite);m.position.copy(b.p).addScaledVector(b.right,side*8.15).addScaledVector(b.normal,.10);orient(m,b);m.castShadow=true;m.receiveShadow=true;world.add(m)}}const dashMat=new THREE.MeshStandardMaterial({color:0xffffff,emissive:0xffffff,emissiveIntensity:.08,roughness:.3}),dashGeo=new THREE.BoxGeometry(.18,.035,2.3);for(let i=3;i<155;i+=3){const b=basisAt(i/158);for(const l of[-2.65,2.65]){const d=new THREE.Mesh(dashGeo,dashMat);d.position.copy(b.p).addScaledVector(b.right,l).addScaledVector(b.normal,.08);orient(d,b);world.add(d)}}
const barrierMat=new THREE.MeshPhysicalMaterial({color:0x157ce5,roughness:.25,metalness:.18,clearcoat:.6,clearcoatRoughness:.2}),barrierGeo=new THREE.BoxGeometry(1.15,.82,.5);for(let i=0;i<220;i++){const b=basisAt(i/219);for(const side of[-1,1]){const m=new THREE.Mesh(barrierGeo,barrierMat);m.position.copy(b.p).addScaledVector(b.right,side*9.55).addScaledVector(b.normal,.39);orient(m,b);m.castShadow=true;world.add(m)}}function addBanner(t,side){const b=basisAt(t),g=new THREE.Group(),poleMat=new THREE.MeshStandardMaterial({color:0x24364c,metalness:.5,roughness:.35}),flagMat=new THREE.MeshPhysicalMaterial({color:0x086be5,roughness:.25,clearcoat:.5}),pole=new THREE.Mesh(new THREE.CylinderGeometry(.12,.16,7,10),poleMat),flag=new THREE.Mesh(new THREE.BoxGeometry(1.55,3.7,.12),flagMat);pole.position.y=3.5;flag.position.set(side*.85,5.1,0);g.add(pole,flag);g.position.copy(b.p).addScaledVector(b.right,side*13);world.add(g)}for(let i=0;i<26;i++)addBanner(.025+i*.037,i%2?1:-1);function addArch(t){const b=basisAt(t),g=new THREE.Group(),mat=new THREE.MeshPhysicalMaterial({color:0x0b70d8,metalness:.28,roughness:.24,clearcoat:.65}),accent=new THREE.MeshBasicMaterial({color:0x53d9ff}),l=new THREE.Mesh(new THREE.BoxGeometry(.8,7.5,.8),mat),r=l.clone(),top=new THREE.Mesh(new THREE.BoxGeometry(19,.8,.8),mat),glow=new THREE.Mesh(new THREE.BoxGeometry(12,.12,.92),accent);l.position.set(-8.8,3.75,0);r.position.set(8.8,3.75,0);top.position.set(0,7.5,0);glow.position.set(0,7.5,.47);g.add(l,r,top,glow);g.position.copy(b.p);g.rotation.y=Math.atan2(b.tangent.x,b.tangent.z);world.add(g)}[.16,.42,.69,.92].forEach(addArch);
function addPalm(t,side,s=.9){const b=basisAt(t),g=new THREE.Group(),trunkMat=new THREE.MeshStandardMaterial({color:0x8e663f,roughness:.9}),leafMat=new THREE.MeshStandardMaterial({color:0x2b9a57,roughness:.8}),trunk=new THREE.Mesh(new THREE.CylinderGeometry(.16,.27,4.8,8),trunkMat);trunk.position.y=2.4;g.add(trunk);for(let i=0;i<7;i++){const leaf=new THREE.Mesh(new THREE.CapsuleGeometry(.18,2.1,4,6),leafMat);leaf.position.y=5;leaf.rotation.z=Math.PI/2.8;leaf.rotation.y=i*Math.PI*2/7;leaf.translateX(1.2);g.add(leaf)}g.position.copy(b.p).addScaledVector(b.right,side*(14+Math.random()*8));g.scale.setScalar(s);world.add(g)}for(let i=0;i<70;i++)addPalm((i+2)/75,i%2?1:-1,.55+Math.random()*.55);const mountainMat=new THREE.MeshStandardMaterial({color:0x6689a9,roughness:1});for(let i=0;i<18;i++){const m=new THREE.Mesh(new THREE.ConeGeometry(22+Math.random()*24,38+Math.random()*42,6),mountainMat);m.position.set(-210+i*27,-1,-250-Math.random()*360);m.rotation.y=Math.random()*Math.PI;m.scale.z=.75+Math.random()*.8;world.add(m)}const cloudMat=new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.82,depthWrite:false});for(let i=0;i<18;i++){const g=new THREE.Group();for(let k=0;k<4;k++){const c=new THREE.Mesh(new THREE.SphereGeometry(6+Math.random()*5,10,8),cloudMat);c.position.set(k*7,Math.random()*4,Math.random()*4);g.add(c)}g.position.set(-180+Math.random()*360,55+Math.random()*35,-120-Math.random()*600);world.add(g)}
const pickupMat=new THREE.MeshPhysicalMaterial({color:0x67e8ff,emissive:0x24a9cf,emissiveIntensity:3.1,roughness:.08,metalness:.15,clearcoat:1}),pickups=[];for(let i=0;i<14;i++){const t=.08+i*.061,b=basisAt(t),mesh=new THREE.Mesh(new THREE.OctahedronGeometry(.92),pickupMat),lane=[-4.2,0,4.2][i%3];mesh.castShadow=true;mesh.position.copy(b.p).addScaledVector(b.right,lane).addScaledVector(b.normal,1.15);world.add(mesh);pickups.push({mesh,t,lane,taken:false})}const particleGeo=new THREE.SphereGeometry(.1,6,4),particles=[];function burst(pos,color,count=22,spread=5){const mat=new THREE.MeshBasicMaterial({color,transparent:true});for(let i=0;i<count;i++){const m=new THREE.Mesh(particleGeo,mat);m.position.copy(pos);world.add(m);particles.push({m,v:new THREE.Vector3((Math.random()-.5)*spread,Math.random()*3,(Math.random()-.5)*spread),life:.7+Math.random()*.5})}}const speedFx=new THREE.Group();camera.add(speedFx);scene.add(camera);const streakMat=new THREE.MeshBasicMaterial({color:0xffffff,transparent:true,opacity:.18,depthTest:false}),streaks=[];for(let i=0;i<62;i++){const s=new THREE.Mesh(new THREE.BoxGeometry(.012,.012,2.4+Math.random()*4),streakMat);s.position.set((Math.random()-.5)*9,(Math.random()-.5)*5,-4-Math.random()*22);speedFx.add(s);streaks.push(s)}
const loader=new GLTFLoader(),CAR_BASE='https://raw.githubusercontent.com/Arslan12216775/kenney_car-kit/master/Models/GLB%20format/',CAR_URLS=[CAR_BASE+'race.glb',CAR_BASE+'race-future.glb',CAR_BASE+'sedan-sports.glb',CAR_BASE+'hatchback-sports.glb',CAR_BASE+'suv-luxury.glb',CAR_BASE+'sedan.glb'],loadGLB=url=>new Promise((resolve,reject)=>loader.load(url,g=>resolve(g.scene),undefined,reject));let cars=[],player,ais=[];function polishCar(model,index){const colors=[0x0869ff,0x753cff,0x17b8c4,0x11151c,0xff3b45,0xffb52d],paint=new THREE.MeshPhysicalMaterial({color:colors[index%colors.length],metalness:.32,roughness:.2,clearcoat:1,clearcoatRoughness:.08,sheen:1,sheenColor:new THREE.Color(0xffffff),sheenRoughness:.25});model.traverse(o=>{if(!o.isMesh)return;o.castShadow=true;o.receiveShadow=true;const n=(o.material?.name||'').toLowerCase();if(n.includes('glass')||n.includes('window'))o.material=new THREE.MeshPhysicalMaterial({color:0x14273d,metalness:.05,roughness:.1,transmission:.16,transparent:true,opacity:.82});else if(n.includes('wheel')||n.includes('tire'))o.material=new THREE.MeshStandardMaterial({color:0x111217,roughness:.76,metalness:.18});else o.material=paint.clone()});const box=new THREE.Box3().setFromObject(model),size=box.getSize(new THREE.Vector3());model.scale.setScalar(4.45/Math.max(size.x,size.z));const box2=new THREE.Box3().setFromObject(model);model.position.y-=box2.min.y;return model}async function loadCars(){const models=await Promise.all(CAR_URLS.map(loadGLB));cars=models.map(polishCar);player=cars[0];world.add(player);ais=cars.slice(1).map((mesh,i)=>({mesh,t:.015+i*.009,lane:[-4,3,-1.5,4,-3][i],pace:.047+Math.random()*.005,stun:0,spin:0,speedScale:1,phase:i*1.7}));ais.forEach(a=>world.add(a.mesh))}
let running=false,targetLane=0,lane=0,playerT=0,boost=0,currentItem=null,last=performance.now(),dragging=false,dragX=0,dragLane=0,missiles=[],mines=[],shake=0,raceTime=0,throttle=0,accelerating=false,braking=false,leftHeld=false,rightHeld=false;function showMsg(s){msgEl.textContent=s;msgEl.classList.add('show');clearTimeout(showMsg.t);showMsg.t=setTimeout(()=>msgEl.classList.remove('show'),800)}function setItem(v){currentItem=v;itemBtn.disabled=!v||!running;itemBtn.textContent=v==='rocket'?'🚀':v==='boost'?'⚡':v==='mine'?'💣':'?'}function placeCar(mesh,t,laneValue){const b=basisAt(t),p=b.p.clone().addScaledVector(b.right,laneValue).addScaledVector(b.normal,.13);mesh.position.copy(p);mesh.rotation.y=Math.atan2(b.tangent.x,b.tangent.z);mesh.rotation.z=-Math.asin(THREE.MathUtils.clamp(b.tangent.y,-1,1))*.35}function reset(){playerT=0;lane=targetLane=0;boost=0;raceTime=0;throttle=0;accelerating=false;braking=false;leftHeld=false;rightHeld=false;shake=0;setItem(null);pickups.forEach(p=>{p.taken=false;p.mesh.visible=true});ais.forEach((a,i)=>{a.t=.015+i*.009;a.stun=0;a.spin=0;a.speedScale=1});missiles.forEach(x=>world.remove(x.mesh));missiles=[];mines.forEach(x=>world.remove(x.mesh));mines=[];running=true;menu.style.display='none';showMsg('HOLD TO ACCELERATE')}function useItem(){if(!running||!currentItem)return;const item=currentItem;setItem(null);if(item==='boost'){boost=1.7;showMsg('TURBO!');burst(player.position,0x55dfff,30,6)}else if(item==='mine'){const b=basisAt(playerT),m=new THREE.Mesh(new THREE.CylinderGeometry(.62,.62,.22,18),new THREE.MeshStandardMaterial({color:0xe44343,emissive:0x5f0909,emissiveIntensity:1.5}));m.position.copy(b.p).addScaledVector(b.right,lane).addScaledVector(b.normal,.18);world.add(m);mines.push({mesh:m,t:playerT,lane,life:14});showMsg('MINE DROPPED')}else{const target=ais.filter(a=>a.t>playerT).sort((a,b)=>a.t-b.t)[0];if(!target){showMsg('NO TARGET');return}const m=new THREE.Mesh(new THREE.CapsuleGeometry(.18,.62,4,8),new THREE.MeshStandardMaterial({color:0xffcc3d,emissive:0xff5a00,emissiveIntensity:3.8}));m.rotation.x=Math.PI/2;m.position.copy(player.position);world.add(m);missiles.push({mesh:m,target,life:3});showMsg('ROCKET!')}}itemBtn.addEventListener('click',e=>{e.stopPropagation();useItem()});playBtn.addEventListener('click',reset);renderer.domElement.addEventListener('pointerdown',e=>{if(!running)return;dragging=true;accelerating=true;braking=false;dragX=e.clientX;dragLane=targetLane;renderer.domElement.setPointerCapture?.(e.pointerId)});renderer.domElement.addEventListener('pointermove',e=>{if(!dragging)return;targetLane=THREE.MathUtils.clamp(dragLane+(e.clientX-dragX)/Math.max(innerWidth,300)*16,-5.2,5.2)});const endPointer=()=>{dragging=false;accelerating=false};renderer.domElement.addEventListener('pointerup',endPointer);renderer.domElement.addEventListener('pointercancel',endPointer);window.addEventListener('keydown',e=>{if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight','Space'].includes(e.code)||['KeyW','KeyA','KeyS','KeyD'].includes(e.code))e.preventDefault();if(e.code==='ArrowUp'||e.code==='KeyW')accelerating=true;if(e.code==='ArrowDown'||e.code==='KeyS')braking=true;if(e.code==='ArrowLeft'||e.code==='KeyA')leftHeld=true;if(e.code==='ArrowRight'||e.code==='KeyD')rightHeld=true;if(e.code==='Space'&&!e.repeat)useItem()},{passive:false});window.addEventListener('keyup',e=>{if(e.code==='ArrowUp'||e.code==='KeyW')accelerating=false;if(e.code==='ArrowDown'||e.code==='KeyS')braking=false;if(e.code==='ArrowLeft'||e.code==='KeyA')leftHeld=false;if(e.code==='ArrowRight'||e.code==='KeyD')rightHeld=false});window.addEventListener('blur',()=>{accelerating=false;braking=false;leftHeld=false;rightHeld=false;dragging=false});
function update(dt){raceTime+=dt;if(leftHeld)targetLane=Math.max(-5.2,targetLane-dt*7.2);if(rightHeld)targetLane=Math.min(5.2,targetLane+dt*7.2);if(accelerating)throttle=Math.min(1,throttle+dt/5);else if(braking)throttle=Math.max(0,throttle-dt*.48);else throttle=Math.max(0,throttle-dt*.12);const launch=throttle*throttle*(3-2*throttle),aiRaw=THREE.MathUtils.clamp(raceTime/5,0,1),aiLaunch=aiRaw*aiRaw*(3-2*aiRaw),speedMul=boost>0?1.22:1;if(boost>0)boost-=dt;const playerPace=THREE.MathUtils.lerp(.004,.055,launch)*speedMul;playerT=Math.min(1,playerT+dt*playerPace);lane+=(targetLane-lane)*Math.min(1,dt*16);placeCar(player,playerT,lane);const turnAmount=THREE.MathUtils.clamp((targetLane-lane)*2.1,-1,1),trackRoll=-Math.asin(THREE.MathUtils.clamp(basisAt(playerT).tangent.y,-1,1))*.35;player.rotation.z+=(trackRoll-turnAmount*.17-player.rotation.z)*Math.min(1,dt*13);ais.forEach(a=>{if(a.speedScale<1)a.speedScale=Math.min(1,a.speedScale+dt*.4);const impact=a.stun>0?.72:1,aiPace=THREE.MathUtils.lerp(a.pace*.38,a.pace,aiLaunch);a.t=Math.min(1,a.t+dt*aiPace*a.speedScale*impact);if(a.stun>0){a.stun-=dt;a.spin+=dt*10}const aiLane=a.lane+Math.sin(a.t*80+a.phase)*1.05;placeCar(a.mesh,a.t,aiLane);if(a.stun>0)a.mesh.rotation.y+=a.spin});pickups.forEach(p=>{p.mesh.rotation.x+=dt*2.5;p.mesh.rotation.y+=dt*3.5;if(!p.taken&&Math.abs(p.t-playerT)<.018&&Math.abs(p.lane-lane)<1.8){p.taken=true;p.mesh.visible=false;if(!currentItem){const r=Math.random();setItem(r<.47?'rocket':r<.75?'boost':'mine');showMsg('ITEM READY');burst(player.position,0x65e6ff,18,3)}}});missiles.forEach(m=>{m.life-=dt;const tp=m.target.mesh.position;m.mesh.position.lerp(tp,.32);const dir=tp.clone().sub(m.mesh.position).normalize();m.mesh.position.addScaledVector(dir,dt*260);m.mesh.rotation.z+=dt*22;if(m.mesh.position.distanceTo(tp)<2.6){m.target.stun=1.2;m.target.speedScale=Math.min(m.target.speedScale,.18);m.life=0;shake=.55;showMsg('DIRECT HIT!');burst(tp,0xffb532,34,7)}});for(let i=missiles.length-1;i>=0;i--)if(missiles[i].life<=0){world.remove(missiles[i].mesh);missiles.splice(i,1)}mines.forEach(m=>{m.life-=dt;ais.forEach(a=>{if(m.life>0&&Math.abs(a.t-m.t)<.018&&Math.abs(a.lane-m.lane)<2){a.stun=1.1;a.speedScale=Math.min(a.speedScale,.28);m.life=0;burst(m.mesh.position,0xff5a44,28,6)}})});for(let i=mines.length-1;i>=0;i--)if(mines[i].life<=0){world.remove(mines[i].mesh);mines.splice(i,1)}particles.forEach(p=>{p.life-=dt;p.m.position.addScaledVector(p.v,dt);p.v.y-=4.8*dt;p.m.material.opacity=Math.max(0,p.life/.8)});for(let i=particles.length-1;i>=0;i--)if(particles[i].life<=0){world.remove(particles[i].m);particles.splice(i,1)}const visualSpeed=THREE.MathUtils.lerp(0,345,launch)*speedMul;speedEl.textContent=Math.round(visualSpeed);rankEl.textContent=[playerT,...ais.map(a=>a.t)].sort((a,b)=>b-a).indexOf(playerT)+1;progressEl.style.width=(playerT*100).toFixed(1)+'%';const b=basisAt(playerT),cameraDistance=THREE.MathUtils.lerp(8.2,10.2,launch),cameraHeight=THREE.MathUtils.lerp(4.7,6.2,launch),behind=b.p.clone().addScaledVector(b.tangent,-cameraDistance).addScaledVector(b.normal,cameraHeight).addScaledVector(b.right,lane*.07),roadBuzz=.035*launch+(boost>0?.035:0);behind.x+=(Math.random()-.5)*roadBuzz;behind.y+=(Math.random()-.5)*roadBuzz;if(shake>0){behind.x+=(Math.random()-.5)*shake;behind.y+=(Math.random()-.5)*shake;shake=Math.max(0,shake-dt*1.9)}camera.position.lerp(behind,1-Math.pow(.000018,dt));camera.lookAt(b.p.clone().addScaledVector(b.tangent,THREE.MathUtils.lerp(31,45,launch)).addScaledVector(b.normal,.75));const targetFov=THREE.MathUtils.lerp(76,88,launch)+(boost>0?7:0);camera.fov+=(targetFov-camera.fov)*dt*9;camera.updateProjectionMatrix();streakMat.opacity=.02+.22*launch+(boost>0?.11:0);streaks.forEach(s=>{s.position.z+=dt*(18+visualSpeed*.17);if(s.position.z>-1){s.position.z=-24-Math.random()*12;s.position.x=(Math.random()-.5)*9;s.position.y=(Math.random()-.5)*5}});if(playerT>=1){running=false;accelerating=false;braking=false;leftHeld=false;rightHeld=false;setItem(null);const final=[playerT,...ais.map(a=>a.t)].sort((a,b)=>b-a).indexOf(playerT)+1;document.querySelector('.title').innerHTML=final===1?'VICTORY!':'FINISHED '+final+'/6';document.querySelector('.subtitle').textContent=final===1?'First place. Smart item timing and clean lines won the race.':'Use pickups more aggressively and save turbo for overtakes.';playBtn.textContent='RACE AGAIN';menu.style.display='flex'}}function animate(now){const dt=Math.min(.033,(now-last)/1000);last=now;if(running)update(dt);else if(player){player.rotation.y+=dt*.1;streakMat.opacity=.015}renderer.render(scene,camera);requestAnimationFrame(animate)}window.addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});(async()=>{try{loadingEl.textContent='Loading polished car models…';await loadCars();placeCar(player,0,0);ais.forEach(a=>placeCar(a.mesh,a.t,a.lane));const b=basisAt(0);camera.position.copy(b.p).addScaledVector(b.tangent,-8.2).addScaledVector(b.normal,4.7);camera.lookAt(b.p.clone().addScaledVector(b.tangent,31));playBtn.disabled=false;playBtn.textContent='START RACE';loadingEl.textContent='Ready'}catch(err){console.error(err);loadingEl.textContent='Could not load remote CC0 car assets. Check internet access.';playBtn.textContent='ASSET LOAD FAILED'}})();requestAnimationFrame(animate);